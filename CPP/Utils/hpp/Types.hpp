#ifndef OPENOMF_UTILS_TYPES_HPP
#define OPENOMF_UTILS_TYPES_HPP

namespace openomf {
namespace utils {

/** Utils 모듈 컴파일/연결 검증용 타입 (추후 allocator, array, str 등으로 확장). */
struct UtilsTypes {};

}  // namespace utils
}  // namespace openomf

#endif
