#ifndef OPENOMF_EXTERNALLIBRARY_CSTDLIB_HPP
#define OPENOMF_EXTERNALLIBRARY_CSTDLIB_HPP

namespace openomf {
namespace external_library {

/** C 표준 라이브러리 래퍼. List.md에 구체 사용 함수 목록이 없어 현재는 래핑 생략. 필요 시 List.md 갱신 후 해당 함수만 추가. */
class CStdLib {
public:
};

} // namespace external_library
} // namespace openomf

#endif // OPENOMF_EXTERNALLIBRARY_CSTDLIB_HPP
