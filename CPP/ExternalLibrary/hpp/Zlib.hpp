#ifndef OPENOMF_EXTERNALLIBRARY_ZLIB_HPP
#define OPENOMF_EXTERNALLIBRARY_ZLIB_HPP

namespace openomf {
namespace external_library {

/** zlib 라이브러리 래퍼. 현재는 libpng 의존 전용이며, zlib API를 직접 사용할 경우 여기에 래퍼를 확장한다. */
class Zlib {
public:
};

} // namespace external_library
} // namespace openomf

#endif // OPENOMF_EXTERNALLIBRARY_ZLIB_HPP
